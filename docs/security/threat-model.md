# Threat Model

This is a lightweight threat model for Seglake.
Update assumptions and add mitigations/tests as the system evolves.

## Scope
- In scope: S3 API, metadata (SQLite), storage engine, replication, ops endpoints.
- Out of scope: cloud infra, OS hardening, TLS termination if handled by external proxy.

## Assumptions (current)
- Deployment exposure: public internet for S3 API; internal-only for /v1/meta/* and /v1/replication/* via proxy allowlist/mTLS. Admin ops channel is local-only (Unix socket).
- Auth: SigV4 required when API keys are configured; public buckets may allow unsigned requests if policy permits.
- TLS: native TLS supported, but deployment expects TLS termination at a proxy; TLS is required at the edge for any public exposure.
- Rate limiting: partial (failed-auth limiter, per-key inflight, MPU Complete limiter; no global RPS limit by default).
- Request limits (recommended): max object size 5 GiB (app); proxy body limit 5.1 GiB; header size 16–32 KB; URL/query length 16–32 KB; timeouts tuned to expected throughput.
- Proxy/WAF global rate limits (baseline): per-IP 200 RPS (burst 400), per-key 500 RPS (burst 1000), global 2000 RPS (burst 4000).
- Replication peers: auth required if API keys exist; repl client can presign via repl-access-key/secret-key.
- Compatibility-first defaults: replay protection is off unless explicitly enabled; Content-MD5 is optional; SigV4 UNSIGNED-PAYLOAD is allowed by default for client compatibility.
- SSE-S3 can be explicit per request or configured as a bucket default. SSE-KMS-compatible `aws:kms` can also be explicit or configured as a bucket default, but maps only to configured Seglake provider key IDs in this phase. Plaintext writes remain the default for buckets without default encryption unless a server-side encryption header is present, but native policies can require effective SSE-S3 or any effective encryption for selected write paths.

## Assets
- Object data (confidentiality, integrity, availability).
- Metadata (policies, versions, oplog, replication state).
- Credentials (API keys, secrets).
- SSE-S3 key-provider access, local KEKs, and wrapped DEK metadata.
- Service availability.

## Threats and Mitigations

| Asset | Entry point | Threat | Example attack | Mitigation | Enforcement | Default | Notes/Config | Test/Verify |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Object data | S3 API (PUT/GET) | Tampering/Info disclosure | Read/write without auth | SigV4; public buckets require explicit allow + policy | code | auth enabled only when keys exist; public buckets off | `-access-key/-secret-key` or stored API keys; `-public-buckets` (default empty) | `internal/s3/e2e_test.go`, `internal/s3/policy_integration_test.go`, `internal/s3/authz_test.go` |
| Metadata | On-disk formats | Tampering/DoS | Corrupt manifest/segment -> crash | Checksums, decoder validation | code | on | N/A | `internal/storage/manifest/codec_fuzz_test.go`, `internal/storage/segment/format_fuzz_test.go`, `internal/storage/segment/index_fuzz_test.go`, `internal/ops/crash_harness_test.go` |
| Credentials | Config/env/API keys | Spoofing/Disclosure | Key leak -> full access | Least privilege; operational rotation and secret handling | code + ops | least privilege on; rotation ops-only | Ops practices in `docs/ops.md` | Ops runbook (`docs/ops.md`) + periodic secrets review |
| Object data / KEKs | Segments/manifests/config | Offline disclosure | Attacker copies data dir without KEKs | Envelope encryption for requests with `x-amz-server-side-encryption: AES256`, `x-amz-server-side-encryption: aws:kms`, or bucket default encryption; key-provider material is external config and is not stored in SQLite/manifests | code + ops | off unless requested per object or configured per bucket | `aws:kms` is provider-key-ID compatibility only, not AWS KMS integration; `-sse-s3-enabled`, `-sse-s3-provider`, `-sse-s3-active-key`, `-sse-s3-kek`, or Vault Transit config; back up local KEKs/Vault separately | `internal/sse`, `internal/storage/engine`, `internal/s3` tests |
| Object data / KEKs | S3 write API | Accidental plaintext writes | Client forgets encryption headers or bucket default is removed | Native policy condition `require_sse_s3: true` rejects non-SSE-S3 writes, and `require_encryption: true` rejects plaintext while allowing SSE-S3 or SSE-KMS-compatible writes; bucket default encryption also satisfies the matching requirement | code | off unless configured in identity or bucket policy | Use bucket/prefix resources to scope enforcement | `internal/s3/policy_test.go`, `internal/s3/policy_integration_test.go` |
| Object data / KEKs | Ops scrub | Silent encrypted corruption | Ciphertext hash still matches rewritten bytes but object no longer decrypts | Optional `scrub -scrub-deep-encrypted` unwraps DEKs and validates AES-GCM tags using configured KEKs, then marks unreadable encrypted versions `DAMAGED` | code + ops | off unless requested | Requires KEKs on the local ops host; diagnostics are redacted | `internal/ops/scrub_test.go` |
| Object data / KEKs | Stats / support bundle | Secret leakage through diagnostics | Operational bundle or stats endpoint exposes key material | SSE diagnostics are metadata-summary only and include counts, key IDs, and short EDEK fingerprint prefixes; they do not read manifests, decrypt objects, or include KEKs, DEKs, raw EDEKs, Vault tokens, wrap nonces, nonce prefixes, or provider secrets | code | on for stats/support-bundle output | Treat key IDs and object counts as operational metadata, not cryptographic secret material | `internal/meta/store_test.go`, `internal/s3/stats_test.go`, `internal/ops/ops_test.go` |
| Object tags | S3 tagging API / support bundle | Info disclosure | Tag presence or values leak across policy boundary or diagnostics | `GetObjectTagging`/`PutObjectTagging`/`DeleteObjectTagging` are policy actions; GET/HEAD tag count is omitted unless tag-read authorization is satisfied; support bundles emit only aggregate tag counts, not keys or values | code | tags off unless clients set them | Treat tags as non-secret metadata; use bucket/prefix policies to scope tag reads and writes | `internal/s3/object_tagging_test.go`, `internal/ops/ops_test.go` |
| Bucket lifecycle config / execution | S3 lifecycle API / replication / ops CLI / diagnostics | Metadata disclosure or unintended retention policy changes | Lifecycle rules reveal prefix/tag structure or lifecycle-run deletes eligible metadata unexpectedly | `GetBucketLifecycle`/`PutBucketLifecycle`/`DeleteBucketLifecycle` are policy actions; config changes replicate as metadata-only oplog entries; lifecycle execution requires saved plan, `-lifecycle-force`, stale-candidate revalidation, and quiesced maintenance; stats and support bundles expose only bucket/rule counts, bucket names, rule IDs, and update times, never XML, filters, tag values, actions, or fingerprints | code | off unless configured and run manually | Treat bucket names and rule IDs as operational metadata; use bucket policies to scope lifecycle reads/writes; review lifecycle plans before running | `internal/s3/bucket_lifecycle_test.go`, `internal/s3/stats_test.go`, `internal/meta/bucket_lifecycle_test.go`, `internal/ops/lifecycle_plan_test.go`, `internal/ops/ops_test.go` |
| Object data / KEKs | Runtime process / Vault token | Key compromise | Compromised host/process reads local KEK, Vault token, or plaintext during request | Operational host hardening; do not log key material; prefer secret manager/file permissions; scope Vault token ACLs to needed Transit operations | ops | N/A | SSE-S3 does not protect against a compromised running Seglake process | Ops runbook (`docs/ops.md`) |
| Object data / Vault provider | S3 encrypted read/write path | Availability | Vault sealed, unavailable, denied, or too slow | Fail closed before committing encrypted writes; encrypted reads validate provider access before streaming bytes; configure timeout and monitor Vault separately | code + ops | provider timeout 5s | `-sse-s3-vault-timeout`; Vault HA/ops outside Seglake | `internal/sse/vault_test.go`, `internal/s3/put_validation_test.go` |
| Object data / KEKs | Rewrap plans / ops CLI | Secret leakage during rotation | Plan file or logs expose DEKs/KEKs/EDEKs | Rewrap plans contain only version metadata, key IDs, key refs, manifest path, and short EDEK fingerprints; rewrap modes are local-only and do not send KEKs through admin socket | code + ops | N/A | `sse-rewrap-plan`, `sse-rewrap-run`; still protect plan files because they reveal object names/key IDs | `internal/ops/sse_rewrap_test.go` |
| Auth (SigV4) | Headers/query | Spoofing/Replay | Bad canonicalization | Strict canonicalization; optional replay cache for presigned requests (opt-in) | code | canonicalization on; replay off | Replay defaults: `-replay-ttl=0`, `-replay-block=false` | `internal/s3/auth_test.go`, `internal/s3/sigv4_test.go`, `internal/s3/handler_replay_test.go`, `internal/s3/replay_test.go`, `internal/s3/auth_fuzz_test.go` |
| Policies | Policy endpoints | Elevation/Disclosure | Policy parse bug bypass; read/modify policies without auth | Validate inputs, deny-by-default; authz on Get/Put/DeleteBucketPolicy | code | on | `GET/PUT/DELETE /<bucket>?policy` requires policy allow | `internal/s3/policy_test.go`, `internal/s3/policy_integration_test.go` |
| Bucket names | ListBuckets (`GET /`) | Info disclosure | Enumerate buckets outside tenant scope | Policy enforcement + per-key bucket allowlist filter | code | allowlist filter applies when configured; otherwise all buckets visible | Allowlist set via `keys allow-bucket` | `internal/s3/policy_integration_test.go` |
| Availability | All inputs | DoS | Large bodies, range explosion | Size/time limits; global RPS limit at proxy/WAF (see recommended limits above) | code + deploy | size limits on; global RPS at proxy | Defaults: `-max-object-size=5GiB`, `-max-url-length=32KiB`, `-max-header-bytes=32KiB`, `-read-timeout=30s`, `-write-timeout=30s`, `-idle-timeout=2m`; Auth limiter 5 req/s burst 5 per IP/key; inflight per-key 32; MPU complete 4. Proxy/WAF RPS/timeout limits per deploy. | `internal/s3/put_validation_test.go`, `internal/s3/ratelimit_test.go` |
| Availability | S3 write ops | Accidental writes during maintenance | Writes during GC/ops window | Maintenance mode (read-only) | code | off | `-mode maintenance -maintenance-action enable` | Add ops validation around maintenance windows |
| Replication | Replication API | Spoofing/Tampering | Fake peer or public exposure of replication endpoints | Require auth (SigV4 when enabled), network allowlist/mTLS at proxy | code + deploy | auth depends on keys; allowlist/mTLS off | Proxy/WAF allowlist/mTLS; SigV4 only when keys exist | `internal/s3/replication_test.go`, `internal/repl/repl_test.go` |
| Ops endpoints | /v1/meta/*, admin socket | Elevation/DoS | Unauth access to ops | Admin socket is local-only + token; /v1/meta/* protected via SigV4 when enabled | code + deploy | auth depends on keys; allowlist/mTLS off for /v1/meta/*; admin socket always local-only | Unix socket + `X-Seglake-Admin-Token` (token file 0600). Proxy/WAF allowlist/mTLS for /v1/meta/* | `internal/s3/policy_integration_test.go` |

## Decisions
- Public exposure is limited to S3 API; /v1/meta/* and /v1/replication/* are internal-only via proxy allowlist/mTLS.
- Request/body limits follow the recommended limits in Assumptions.
- TLS requirement is edge-only (proxy or native TLS); internal HTTP behind the proxy is acceptable.
- Proxy/WAF global rate limits follow the baseline values in Assumptions (deployment may tune).
 - Compatibility-first defaults are used for client-facing auth features; hardening knobs are opt-in per deployment.
